name: Deploy Agent Shelf to Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true
      - name: Verify imported skills against pinned upstream bytes
        run: node tools/verify-imported-skills.mjs
      - name: Upload static site
        uses: actions/upload-pages-artifact@v4
        with:
          path: .
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Smoke test published catalog
        shell: bash
        run: |
          base="${{ steps.deployment.outputs.page_url }}"
          url="${base%/}/catalog.json"
          echo "Checking $url"
          for attempt in {1..12}; do
            if body="$(curl --fail --silent --show-error --location --max-time 15 "$url")" && grep -q '"items"' <<<"$body"; then
              echo "Published Agent Shelf catalog is reachable."
              exit 0
            fi
            echo "Attempt $attempt failed; waiting for Pages propagation..."
            sleep 5
          done
          echo "Published catalog smoke test failed."
          exit 1
